apiVersion: v1
kind: ConfigMap
metadata:
  name: quak-config
  labels:
    app.kubernetes.io/name: quak
    app.kubernetes.io/component: server
data:
  NODE_ENV: "production"
  PORT: "3001"
  STORAGE_DIR: "/var/lib/quak"
  MOCK_LLM: "false"
